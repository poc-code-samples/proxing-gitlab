{
  "name": "http-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx rimraf ./dist && npm run transpile && cp -r ./certs ./dist/",
    "start": "node ./dist/src/index.js",
    "transpile": "npx tsc",
    "dev": "npm run build && npm run start",
    "test": "npm run build && npx jasmine",
    "test:alone": "npx jasmine"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/http-proxy": "^1.17.15",
    "@types/jasmine": "^5.1.4",
    "@types/node": "^22.7.4",
    "jasmine": "^5.3.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
